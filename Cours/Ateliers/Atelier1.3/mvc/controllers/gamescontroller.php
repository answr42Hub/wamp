<?php
require '../models/games.php';
require '../models/category.php';
class gamescontroller {
/*
    function loadGames(): games[] {

    }

    function addGame(): void {
        $bd = new PDO('mysql:dbname=test;host=172.18.0.2;port=3306',
            'root', 'root');
        $game = $_POST[''];

        $bd = new PDO('mysql:dbname=test;host=172.18.0.2;port=3306',
            'root', 'root');
        $games = $bd->query("SELECT * FROM games INNER JOIN categories WHERE id_cat = cat_id");
        $games->execute();

        if(isset($_POST['btnDel'])) {
            $query = $bd->prepare("DELETE FROM games WHERE id_game = ?;");
            $query->bindValue(1, $_POST['btnDel'], PDO::PARAM_STR);
            $query->execute();
            $game = $query->fetch(PDO::FETCH_OBJ);
            header("Location: index.php");
        }

    }

    function editGame(): void {

    }

*/
}